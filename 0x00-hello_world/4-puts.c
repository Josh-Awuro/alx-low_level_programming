#include <stdio.h>

/**
 * main - Entry point for the code
 *
 * Return: success (0)
 */
int main(void)
{
	char str[] = "\"Programming is like building a multilingual puzzle\n";

	puts(str);

	return (0);
}
