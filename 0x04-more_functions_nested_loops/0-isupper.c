#include "main.h"

/**
 * _isupper - checks for uppercase characters
 * @c: takes the chacrater
 *
 * Return: 1 if upper and 0 if otherwise
 */
int _isupper(int c)
{
	if (isupper(c))
		return (1);
	else
		return (0);
}
